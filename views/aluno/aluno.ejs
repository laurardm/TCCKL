<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Editar Aluno</title>
  <link rel="stylesheet" href="/css/turmas.css" />
</head>
<body>
  <h1>Editar Aluno</h1>
  <div class="editar-container">
    <img
      id="fotoAluno"
      src="<%= aluno.foto ? aluno.foto : '/imagens/perfil.png' %>"
      alt="Foto do Aluno"
      style="width: 120px; height: 120px; object-fit: cover; border-radius: 8px;"
    />
    <br />

    <% if (tipoUsuario === 'funcionario') { %>
      <input type="file" id="inputFoto" accept="image/*" />
      <br /><br />
      <label for="nomeAluno">Nome:</label>
      <input type="text" id="nomeAluno" value="<%= aluno.nome %>" />
      <br /><br />
      <button id="salvarAluno" data-cod="<%= aluno.cod %>" data-turma="<%= aluno.nome_turma %>">Salvar</button>
    <% } else { %>
      <p><strong>Nome:</strong> <%= aluno.nome %></p>
    <% } %>

    <a href="/turmas/<%= aluno.nome_turma %>">
      <button type="button">Voltar</button>
    </a>
  </div>

  <% if (tipoUsuario === 'funcionario') { %>
    <script src="/js/aluno.js"></script>
  <% } %>
</body>
</html>
