<!DOCTYPE html>
<html lang="pt-BR">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Perfil do Responsável</title>
  <link rel="stylesheet" href="/css/perfil.css" />
  <link rel="stylesheet" href="/css/footer.css" />
  <link rel="stylesheet" href="/css/logo.css" />
</head>

<body>
  <div class="foto">
    <div class="fotoperfil">
      <img src="<%= foto ? '/uploads/' + foto : '/imagens/perfil.png' %>" alt="Foto do usuário" />
      <p id="nomeDisplay">
        <%= nome %>
      </p>
    </div>
    <div class="form-container">
      <% if (erro) { %>
        <div class="erro-msg" style="color:red; margin-bottom: 10px;">
          <%= erro %>
        </div>
        <% } %>
          <form id="perfilForm" method="POST" action="/perfilr" enctype="multipart/form-data">
            <label for="nome">Nome</label>
            <input type="text" id="nome" name="nome" value="<%= nome %>" disabled required />

            <label for="data">Data de nascimento</label>
            <input type="date" id="data" name="data_nasc" value="<%= data_nasc %>" disabled required />

            <label for="email">E-mail</label>
            <input type="email" id="email" name="email" value="<%= email %>" disabled required />

            <label for="parentesco">Parentesco</label>
            <select id="parentesco" name="parentesco" disabled required>
              <option value="1" <%=parentesco==='1' ? 'selected' : '' %>>Avó</option>
              <option value="2" <%=parentesco==='2' ? 'selected' : '' %>>Avô</option>
              <option value="3" <%=parentesco==='3' ? 'selected' : '' %>>Irmão</option>
              <option value="4" <%=parentesco==='4' ? 'selected' : '' %>>Pai</option>
              <option value="5" <%=parentesco==='5' ? 'selected' : '' %>>Mãe</option>
              <option value="6" <%=parentesco==='6' ? 'selected' : '' %>>Outros</option>
            </select>



            <label for="foto">Foto de perfil</label>
            <input type="file" id="foto" name="foto" accept="image/*" disabled />

            <div class="botoes">
              <button type="button" id="voltar">Voltar</button>
              <button type="button" id="editar">Editar</button>
              <button type="submit" id="salvar" disabled>Salvar</button>
            </div>
          </form>
    </div>
  </div>
  <div id="footer"></div>
  <div id="logo"></div>

  <script src="/js/perfil.js"></script>
  <script src="/js/footer.js"></script>
  <script src="/js/logo.js"></script>
</body>

</html>